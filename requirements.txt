fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy==2.0.30
pyjwt==2.8.0
python-multipart==0.0.9
slowapi==0.1.9
telethon==1.34.0
rapidfuzz==3.9.3
python-dotenv==1.0.1
psycopg2-binary==2.9.9
bcrypt==4.1.3
scipy>=1.11.0  # Hungarian assignment for linker.py (graceful fallback if absent)
Pillow>=10.0.0  # Image preprocessing for OCR (graceful fallback if absent)
google-cloud-vision>=3.0.0  # Cloud Vision OCR for lot number extraction (graceful fallback if absent)
# pytesseract>=0.3.10  # Tesseract alternative — not used (Cloud Vision preferred)
